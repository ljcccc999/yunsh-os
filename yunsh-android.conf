# Android runtime policy for the Raspberry Pi 5 image.
# The builder should inject arm64 system.img and vendor.img into this path.
preloaded_images=/etc/waydroid-extra/images
allow_online_init=false
fallback_store=fdroid
